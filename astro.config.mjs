import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace with your GitHub username
  site: 'https://Chen-Zhao-Sheridan.github.io', 
  // Replace with your repository name (starting with a slash)
  base: '/AstroQuest', 
});